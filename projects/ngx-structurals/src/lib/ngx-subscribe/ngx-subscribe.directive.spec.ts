import { NgxSubscribeDirective } from './ngx-subscribe.directive';

describe('NgxSubscribeDirective', () => {
    it('should create an instance', () => {
        const directive = new NgxSubscribeDirective();
        expect(directive).toBeTruthy();
    });
});
